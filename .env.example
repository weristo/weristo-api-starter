# Tenant-owned AI keys only. Never put platform owner keys into tenant traffic.
TENANT_LAMBDA_AI_KEY=replace-with-tenant-owned-key

# Optional notification provider credentials controlled by the API user.
TENANT_TELEGRAM_BOT_TOKEN=replace-with-tenant-owned-token
TENANT_EMAIL_API_KEY=replace-with-tenant-owned-email-key
